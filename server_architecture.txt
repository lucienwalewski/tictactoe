while (not terminated):
    Get player sockaddr of the player who's turn it is
    Tell him to make his move ("FYI" then "MYM") 
    Wait for his response with recvfrom 
    Make sure it is the correct address and he replied with "MOV"
    if (incorrect address):
        Reply to the third "new" player with "END"
    Process the move -> ensure it is legal, update the board etc.
    if (not a valid move):
        Notify the player with a TXT message
    else: 
        Continue
    if (game has terminated (with a function)):
        Send End to both players indicating the winnder
        Set terminated to True

// Once finished

Reinstantiate the thing

    